#Using Open
#Before we can read the file, we must tell python which file and what we will do

#handle = open(filename,mode)
#returns a handle use to manipulate the file
#filename is a string
#mode is optional and should be 'r' if we are planning to read the file and 'w' if we are going to write to teh file


#Newline character
#Newline is still one character not two
stuff = "hello \nWorld!"
print(stuff)
print(len(stuff))